核技巧（Kernel Trick）用于隐式高维映射：
- 不直接计算 φ(x)，而计算核函数 K(x_i, x_j)=<φ(x_i), φ(x_j)>

常见核函数：
1) 多项式核：K(x, z)=(x^T z + c)^d
2) 高斯核（RBF）：K(x, z)=exp(-γ ||x-z||^2)

直观理解：
- 二维中不可分的数据，经过映射到更高维后，可能被线性超平面分开
- 本实验通过z轴函数变换进行可视化演示该思想